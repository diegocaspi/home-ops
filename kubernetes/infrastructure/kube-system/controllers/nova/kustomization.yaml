apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

configMapGenerator:
  - name: cilium-values
    files:
      - values.yaml=./cilium.values.yaml

generatorOptions:
  disableNameSuffixHash: true
